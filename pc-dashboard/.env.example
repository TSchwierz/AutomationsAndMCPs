VITE_PI_API_BASE=http://127.0.0.1:8787
VITE_API_TOKEN=dev-token
# Server-only (Vite middleware). Do not prefix with VITE_ — that would leak to the browser.
MISTRAL_API_KEY=
# Optional override; default mistral-small-latest
# MISTRAL_MODEL=mistral-small-latest
